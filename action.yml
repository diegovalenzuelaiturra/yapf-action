# action.yml
name: "YAPF Formatter"
description: "Runs YAPF code formatter for Python."
inputs:
  args: # id of input
    description: "yapf args"
    required: true
    default: . --in-place --recursive
  
runs:
  using: "docker"
  image: "Dockerfile"
